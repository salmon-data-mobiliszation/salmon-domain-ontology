name: Ontology CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  validation:
    name: Validate committed ontology artifacts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python deps
        run: |
          pip install rdflib pyshacl

      - name: Run validation bundle
        run: |
          make test

  reasoner-gate:
    name: Reasoner gate (ELK)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Install ROBOT
        run: |
          make install-robot

      - name: Run ELK over the modular build
        run: |
          make verify-reasoner

  publication-surface-drift-check:
    name: Verify published docs artifacts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Install Python deps
        run: |
          pip install rdflib

      - name: Install docs build tools
        run: |
          make install-robot
          make install-widoco

      - name: Refresh docs and check for drift
        run: |
          make verify-generated-artifacts
